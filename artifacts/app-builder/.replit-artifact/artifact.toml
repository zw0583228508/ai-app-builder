kind = "web"
previewPath = "/"
title = "AI App Builder"
version = "1.0.0"
id = "artifacts/app-builder"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20311

[services.development]
run = "pnpm --filter @workspace/app-builder run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/app-builder", "run", "build" ]
publicDir = "artifacts/app-builder/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20311"
BASE_PATH = "/"
